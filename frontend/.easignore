# Native projects are generated by EAS from app.json (prebuild) — uploading the
# local copies ships hundreds of MB of build output and slows/breaks the upload.
android/
ios/

# Local build + export output
dist/
.expo/
web-build/

# Misc
*.apk
*.aab
*.keystore
